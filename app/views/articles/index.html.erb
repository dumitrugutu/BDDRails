<%= link_to "New Article", new_article_path %>

<% if @articles.empty? %>
  <h1 id="no-articles">There aren't any articles</h1>
<% else %>
  <% @articles.each do |article| %>
  <h2><%= link_to article.title, article_path(article) %></h2>
  <p><%= article.body %></p>
  <% end %>
<% end %>
